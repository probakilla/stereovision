#include "robot_controller.h"


double calc_dist (cv::Mat const & depth_map)
{
    // Todo.
    return 0;
}

void process(const cv::Mat & left_img, const cv::Mat & right_img, float * vx, float * vy, float * omega)
{
// Todo.
}
